{
  "servers": [
    {
      "name": "filesystem",
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "./workspace"
      ],
      "enabled": true,
      "description": "Akses filesystem workspace"
    },
    {
      "name": "openbrowser",
      "type": "stdio",
      "command": "python3",
      "args": [
        "-c",
        "from openbrowser.cli import main; main()",
        "--mcp"
      ],
      "enabled": false,
      "description": "AI-native browser automation server (OpenBrowser) — dinonaktifkan, gunakan built-in fetch_url & navigate_web"
    }
  ]
}
